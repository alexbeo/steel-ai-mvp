version: "3.9"

services:
  steel-ai:
    build: .
    # FastAPI on :8000 (Streamlit on :8501 was decommissioned in PR 13).
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./reports:/app/reports
      - ./decision_log:/app/decision_log
    environment:
      - PYTHONPATH=/app
      # - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}  # опционально для Critic LLM
    restart: unless-stopped
